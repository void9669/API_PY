# ==========================================
# FIX: добавлены минимальные версии (>=).
# Раньше не было пинning — при установке на другую машину
# могла поставиться несовместимая версия.
# ==========================================
allure-pytest>=2.13.0
requests>=2.28.0
pytest>=7.0.0
